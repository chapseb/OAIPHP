{% extends 'master.twig' %}

{% block body %}
    <div id="wrapper">
        {{include('admin/topbar.twig')}}
        {{include('admin/sidebar.twig')}}
        <div id="page-wrapper">
            {% block content %}
                {% if template is defined %}
                    {% include template with {'user': user}%}
                {% else %}
                    {% include 'admin/dashboard.twig' with{'user': user} %}
                {% endif %}
            {% endblock %}
        </div>
        <!-- /#page-wrapper -->

    </div>
    <!-- /#wrapper -->
{% endblock %}
